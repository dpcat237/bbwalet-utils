# AI Agent Specifications - vendored plugin copy (.agents/.ai-spec/)
.agents/.ai-spec/

# Go build output
/bin/

# developer-local env overrides
.env.local
# shared dev overrides — gitignored because it may hold a real Wallet API token
.env.dev
# disposable-Wallet token for the gated `make test-e2e` lane
.env.e2e
# logs
out